name: Bot Pogodowy

permissions:
  contents: write   # zezwala na push plików
  pages: write      # zezwala na publikację stron
  id-token: write   # potrzebne, jeśli robimy auth OIDC (opcjonalnie)

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 6 * * *'  # 06:00 UTC = 08:00 CEST

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run bot
        run: python bot.py

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./
